# configuration-guidelines

`rule-config.json` conventions for `autoins` — split out from `application-architecture` since it has a
different lifecycle/owner and maps directly to the config-generator subagent's dedicated grounding
context.

Status: scaffold only, content not yet curated.
